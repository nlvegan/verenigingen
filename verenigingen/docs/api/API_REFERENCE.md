# Verenigingen API Documentation

Generated on: {datetime.now().strftime('%Y-%m-%d %H:%M:%S')}

## Table of Contents

- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})
- [{file_name}](#{file_name.replace('_', '-')})

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| filters | string | Yes | Report filters dict |

**Example Request:**
```json
{
  "filters": "example_filters"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| from_date | string | Yes | Start date for report |
| to_date | string | Yes | End date for report |
| include_bsn | string | No | Whether to include decrypted BSN/RSIN (requires special permission) |

**Example Request:**
```json
{
  "from_date": "example_from_date",
  "to_date": "example_to_date",
  "include_bsn": "example_include_bsn"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| from_date | string | No | Optional start date |
| to_date | string | No | Optional end date |

**Example Request:**
```json
{
  "from_date": "example_from_date",
  "to_date": "example_to_date"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| donor | string | Yes | Donor document name |

**Example Request:**
```json
{
  "donor": "example_donor"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| filters | string | No | Optional filters dict |

**Example Request:**
```json
{
  "filters": "example_filters"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| donor | string | Yes | Donor document name |
| consent | string | Yes | Boolean indicating consent status |
| reason | string | No | Optional reason for consent change |

**Example Request:**
```json
{
  "donor": "example_donor",
  "consent": "example_consent",
  "reason": "example_reason"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| bsn | string | Yes | BSN number to validate |

**Example Request:**
```json
{
  "bsn": "example_bsn"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| job_id | string | Yes | Unique job identifier |

**Example Request:**
```json
{
  "job_id": "example_job_id"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| days_to_keep | integer | No | Number of days to keep job records (default 7) |

**Example Request:**
```json
{
  "days_to_keep": 1
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| job_id | string | Yes | Unique job identifier |

**Example Request:**
```json
{
  "job_id": "example_job_id"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| user | string | No | User to get jobs for (defaults to current session user) |
| status_filter | string | No | Filter by job status (Queued, Running, Completed, Failed) |
| job_type_filter | string | No | Filter by job type |
| limit | integer | No | Maximum number of jobs to return (default 50, max 100) |

**Example Request:**
```json
{
  "user": "example_user",
  "status_filter": "example_status_filter",
  "job_type_filter": "example_job_type_filter",
  "limit": 1
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| job_id | string | Yes | Unique job identifier |

**Example Request:**
```json
{
  "job_id": "example_job_id"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| invalidation_jobs | array | Yes | List of invalidation job configurations |
| delay_seconds | integer | No | Delay before executing invalidation |

**Example Request:**
```json
{
  "invalidation_jobs": [],
  "delay_seconds": 1
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| doctype | string | Yes | Document type that changed |
| doc_name | string | Yes | Name of the changed document |
| change_type | string | No | Type of change (insert, update, delete) |
| changed_fields | array | No | List of changed fields (for updates) |

**Example Request:**
```json
{
  "doctype": "example_doctype",
  "doc_name": "John Doe",
  "change_type": "example_change_type",
  "changed_fields": []
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| doctype | string | No | DocType to validate (optional) |
| doc_name | string | No | Specific document to validate (optional) |

**Example Request:**
```json
{
  "doctype": "example_doctype",
  "doc_name": "John Doe"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    },
    "data": {
      "type": "object"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    },
    "data": {
      "type": "object"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    },
    "data": {
      "type": "object"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    },
    "data": {
      "type": "object"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    },
    "data": {
      "type": "object"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    },
    "data": {
      "type": "object"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    },
    "data": {
      "type": "object"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    },
    "data": {
      "type": "object"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    },
    "data": {
      "type": "object"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    },
    "data": {
      "type": "object"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| import_name | string | Yes |  |

**Example Request:**
```json
{
  "import_name": "John Doe"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| import_name | string | Yes |  |

**Example Request:**
```json
{
  "import_name": "John Doe"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| chapter | string | No |  |

**Example Request:**
```json
{
  "chapter": "example_chapter"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| chapter | string | No |  |

**Example Request:**
```json
{
  "chapter": "example_chapter"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| chapter | string | No |  |

**Example Request:**
```json
{
  "chapter": "example_chapter"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| chapter_name | string | Yes |  |

**Example Request:**
```json
{
  "chapter_name": "John Doe"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| chapter | string | No |  |

**Example Request:**
```json
{
  "chapter": "example_chapter"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| chapter | string | No |  |

**Example Request:**
```json
{
  "chapter": "example_chapter"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| chapter | string | No |  |

**Example Request:**
```json
{
  "chapter": "example_chapter"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| chapter | string | No |  |

**Example Request:**
```json
{
  "chapter": "example_chapter"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| func | string | Yes |  |

**Example Request:**
```json
{
  "func": "example_func"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    },
    "data": {
      "type": "object"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| import_name | string | Yes |  |

**Example Request:**
```json
{
  "import_name": "John Doe"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| chapter_name | string | Yes |  |

**Example Request:**
```json
{
  "chapter_name": "John Doe"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| chapter_name | string | Yes |  |
| introduction | string | Yes |  |

**Example Request:**
```json
{
  "chapter_name": "John Doe",
  "introduction": "example_introduction"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| issues | string | Yes |  |

**Example Request:**
```json
{
  "issues": "example_issues"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| company | string | Yes |  |

**Example Request:**
```json
{
  "company": "example_company"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| force_enable | boolean | No |  |

**Example Request:**
```json
{
  "force_enable": true
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| member_name | string | Yes |  |

**Example Request:**
```json
{
  "member_name": "John Doe"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| member_name | string | Yes |  |

**Example Request:**
```json
{
  "member_name": "John Doe"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| member_name | string | Yes |  |

**Example Request:**
```json
{
  "member_name": "John Doe"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| operation | string | Yes |  |

**Example Request:**
```json
{
  "operation": "example_operation"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| member_name | string | Yes |  |

**Example Request:**
```json
{
  "member_name": "John Doe"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| member_name | string | Yes |  |

**Example Request:**
```json
{
  "member_name": "John Doe"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| member_name | string | Yes |  |

**Example Request:**
```json
{
  "member_name": "John Doe"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| token | string | Yes | CSRF token to validate |

**Example Request:**
```json
{
  "token": "example_token"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| email | string | Yes |  |

**Example Request:**
```json
{
  "email": "user@example.com"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| chapter_name | string | Yes |  |

**Example Request:**
```json
{
  "chapter_name": "John Doe"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    },
    "data": {
      "type": "object"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    },
    "data": {
      "type": "object"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    },
    "data": {
      "type": "object"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    },
    "data": {
      "type": "object"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    },
    "data": {
      "type": "object"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    },
    "data": {
      "type": "object"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    },
    "data": {
      "type": "object"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    },
    "data": {
      "type": "object"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| customer | string | Yes |  |

**Example Request:**
```json
{
  "customer": "example_customer"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    },
    "data": {
      "type": "object"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| duration_hours | integer | No |  |

**Example Request:**
```json
{
  "duration_hours": 1
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| member_name | string | Yes |  |

**Example Request:**
```json
{
  "member_name": "John Doe"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| member_name | string | Yes |  |

**Example Request:**
```json
{
  "member_name": "John Doe"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| member_name | string | Yes |  |

**Example Request:**
```json
{
  "member_name": "John Doe"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| schedule_name | string | No |  |

**Example Request:**
```json
{
  "schedule_name": "John Doe"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| error_log_name | string | Yes |  |

**Example Request:**
```json
{
  "error_log_name": "John Doe"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| member_name | string | Yes |  |

**Example Request:**
```json
{
  "member_name": "John Doe"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| member_name | string | Yes |  |

**Example Request:**
```json
{
  "member_name": "John Doe"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| invoice_name | string | Yes |  |

**Example Request:**
```json
{
  "invoice_name": "John Doe"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| member_name | string | No |  |

**Example Request:**
```json
{
  "member_name": "John Doe"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| member_name | string | No |  |

**Example Request:**
```json
{
  "member_name": "John Doe"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| member_name | string | No |  |

**Example Request:**
```json
{
  "member_name": "John Doe"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| func | string | Yes |  |

**Example Request:**
```json
{
  "func": "example_func"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| func | string | Yes |  |

**Example Request:**
```json
{
  "func": "example_func"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    },
    "data": {
      "type": "object"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| donations_account | integer | No | GL account to check (optional) |
| date_from | string | No | Start date for processing (optional) |
| date_to | string | No | End date for processing (optional) |

**Example Request:**
```json
{
  "donations_account": 1,
  "date_from": "example_date_from",
  "date_to": "example_date_to"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| customer_name | string | Yes | Customer to simulate for |
| amount | number | Yes | Donation amount |
| donations_account | integer | No | GL account to use (optional) |

**Example Request:**
```json
{
  "customer_name": "John Doe",
  "amount": 10.0,
  "donations_account": 1
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| customer_name | string | Yes | Customer to test |
| amount | number | Yes | Donation amount to test |

**Example Request:**
```json
{
  "customer_name": "John Doe",
  "amount": 10.0
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| enabled | boolean | Yes | Boolean to enable/disable auto-creation |
| donations_gl_account | integer | No | GL Account for donations |
| eligible_customer_groups | string | No | Comma-separated customer groups |
| minimum_amount | number | No | Minimum donation amount threshold |

**Example Request:**
```json
{
  "enabled": true,
  "donations_gl_account": 1,
  "eligible_customer_groups": "example_eligible_customer_groups",
  "minimum_amount": 10.0
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| donor_name | string | Yes | Name of the donor record |

**Example Request:**
```json
{
  "donor_name": "John Doe"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| donor_name | string | Yes | Name of the donor record |

**Example Request:**
```json
{
  "donor_name": "John Doe"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| donor_name | string | Yes | Name of the donor record |
| remove_customer | string | No | Whether to also delete the customer record |

**Example Request:**
```json
{
  "donor_name": "John Doe",
  "remove_customer": "example_remove_customer"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    },
    "data": {
      "type": "object"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    },
    "data": {
      "type": "object"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| template_name | string | Yes |  |
| test_context | string | No |  |

**Example Request:**
```json
{
  "template_name": "John Doe",
  "test_context": "example_test_context"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| job_id | string | Yes | Job ID to cancel |

**Example Request:**
```json
{
  "job_id": "example_job_id"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| member_name | string | Yes | Member to refresh payment history for |
| priority | string | No | Job priority level |

**Example Request:**
```json
{
  "member_name": "John Doe",
  "priority": "example_priority"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| sample_size | integer | No | Number of members to analyze |
| priority | string | No | Job priority level |

**Example Request:**
```json
{
  "sample_size": 1,
  "priority": "example_priority"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| job_function | string | Yes | Function name to execute |
| job_args | object | No | Arguments for the job function |
| priority | string | No | Job priority (critical, high, normal, low, bulk) |
| operation_type | string | No | Security operation type |
| estimated_duration | integer | No | Estimated execution time in seconds |

**Example Request:**
```json
{
  "job_function": "example_job_function",
  "job_args": {},
  "priority": "example_priority",
  "operation_type": "example_operation_type",
  "estimated_duration": 1
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| job_id | string | Yes | Job ID to check |

**Example Request:**
```json
{
  "job_id": "example_job_id"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| membership_type | string | No |  |

**Example Request:**
```json
{
  "membership_type": "example_membership_type"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| func | string | Yes |  |

**Example Request:**
```json
{
  "func": "example_func"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| member_name | string | No |  |

**Example Request:**
```json
{
  "member_name": "John Doe"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| member_name | string | No |  |

**Example Request:**
```json
{
  "member_name": "John Doe"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    },
    "data": {
      "type": "object"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    },
    "data": {
      "type": "object"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| invoice_name | string | Yes |  |

**Example Request:**
```json
{
  "invoice_name": "John Doe"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| days_back | string | No |  |

**Example Request:**
```json
{
  "days_back": "example_days_back"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| schedule_name | string | Yes |  |

**Example Request:**
```json
{
  "schedule_name": "John Doe"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| schedule_name | string | Yes |  |
| force | string | No |  |

**Example Request:**
```json
{
  "schedule_name": "John Doe",
  "force": "example_force"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| force_enable | boolean | No |  |

**Example Request:**
```json
{
  "force_enable": true
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| force_enable | boolean | No |  |

**Example Request:**
```json
{
  "force_enable": true
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| schedule_name | string | Yes |  |
| force | string | No |  |

**Example Request:**
```json
{
  "schedule_name": "John Doe",
  "force": "example_force"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| payment_entry | string | Yes |  |
| invoice_type | string | Yes |  |
| invoice_name | string | Yes |  |

**Example Request:**
```json
{
  "payment_entry": "example_payment_entry",
  "invoice_type": "example_invoice_type",
  "invoice_name": "John Doe"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| older_than_hours | integer | No | Clear jobs older than this many hours |

**Example Request:**
```json
{
  "older_than_hours": 1
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| job_name | string | Yes | Name/ID of the job to check |

**Example Request:**
```json
{
  "job_name": "John Doe"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| member | string | No | Optional member name to filter by |
| limit | integer | No | Maximum number of jobs to return |

**Example Request:**
```json
{
  "member": "example_member",
  "limit": 1
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| limit | integer | No | Maximum number of jobs to return |

**Example Request:**
```json
{
  "limit": 1
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| job_name | string | Yes | Name/ID of the job to retry |

**Example Request:**
```json
{
  "job_name": "John Doe"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| member_name | string | Yes |  |

**Example Request:**
```json
{
  "member_name": "John Doe"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| member_name | string | Yes | Name of the Member record |

**Example Request:**
```json
{
  "member_name": "John Doe"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| member_name | string | Yes |  |
| chapter_name | string | Yes |  |

**Example Request:**
```json
{
  "member_name": "John Doe",
  "chapter_name": "John Doe"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| assignments | string | Yes | List of dicts with member_name and chapter_name |

**Example Request:**
```json
{
  "assignments": "example_assignments"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| member_id | integer | Yes |  |

**Example Request:**
```json
{
  "member_id": 1
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    },
    "data": {
      "type": "object"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| member_id | integer | Yes |  |

**Example Request:**
```json
{
  "member_id": 1
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    },
    "data": {
      "type": "object"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| iban | string | Yes |  |

**Example Request:**
```json
{
  "iban": "example_iban"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| file_content_b64 | string | Yes |  |
| bank_account | integer | Yes |  |
| company | string | No |  |

**Example Request:**
```json
{
  "file_content_b64": "example_file_content_b64",
  "bank_account": 1,
  "company": "example_company"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| file_content | string | Yes |  |
| bank_account | integer | No |  |

**Example Request:**
```json
{
  "file_content": "example_file_content",
  "bank_account": 1
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| file_content | string | Yes |  |
| bank_account | integer | No |  |
| company | string | No |  |

**Example Request:**
```json
{
  "file_content": "example_file_content",
  "bank_account": 1,
  "company": "example_company"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| file_content | string | Yes |  |
| bank_account | integer | No |  |

**Example Request:**
```json
{
  "file_content": "example_file_content",
  "bank_account": 1
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| member_id | integer | Yes |  |

**Example Request:**
```json
{
  "member_id": 1
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    },
    "data": {
      "type": "object"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| chapter_name | string | Yes |  |

**Example Request:**
```json
{
  "chapter_name": "John Doe"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| filters | string | No | Optional dict with status, chapter_name, etc. |
| limit | string | No | Maximum members to return (default 50) |

**Example Request:**
```json
{
  "filters": "example_filters",
  "limit": "example_limit"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| kwargs | string | Yes |  |

**Example Request:**
```json
{
  "kwargs": "example_kwargs"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| func | string | Yes |  |

**Example Request:**
```json
{
  "func": "example_func"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| file_content | string | Yes |  |
| bank_account | integer | No |  |
| company | string | No |  |

**Example Request:**
```json
{
  "file_content": "example_file_content",
  "bank_account": 1,
  "company": "example_company"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| member_id | integer | Yes |  |

**Example Request:**
```json
{
  "member_id": 1
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    },
    "data": {
      "type": "object"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| file_content | string | Yes |  |
| bank_account | integer | No |  |

**Example Request:**
```json
{
  "file_content": "example_file_content",
  "bank_account": 1
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| member_id | integer | Yes |  |

**Example Request:**
```json
{
  "member_id": 1
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| schema_name | string | Yes |  |
| expose_errors | boolean | No |  |

**Example Request:**
```json
{
  "schema_name": "John Doe",
  "expose_errors": true
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| members_data | string | Yes | JSON string containing array of member data |

**Example Request:**
```json
{
  "members_data": "example_members_data"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    },
    "data": {
      "type": "object"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| member_name | string | No | Specific member to clear cache for, or None for all |

**Example Request:**
```json
{
  "member_name": "John Doe"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| member_data | string | Yes | JSON string containing member information |

**Example Request:**
```json
{
  "member_data": "example_member_data"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    },
    "data": {
      "type": "object"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| member_name | string | Yes | Name of the member |

**Example Request:**
```json
{
  "member_name": "John Doe"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| filters | string | No | JSON string with search filters |
| limit | integer | No | Maximum number of results to return |

**Example Request:**
```json
{
  "filters": "example_filters",
  "limit": 1
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| member_name | string | Yes |  |
| notes | string | No |  |

**Example Request:**
```json
{
  "member_name": "John Doe",
  "notes": "example_notes"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| data | string | Yes |  |

**Example Request:**
```json
{
  "data": "example_data"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| data | string | Yes |  |

**Example Request:**
```json
{
  "data": "example_data"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| application_id | integer | Yes |  |

**Example Request:**
```json
{
  "application_id": 1
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| application_id | integer | Yes |  |

**Example Request:**
```json
{
  "application_id": 1
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| member_name | string | No |  |

**Example Request:**
```json
{
  "member_name": "John Doe"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| member_name | string | Yes |  |
| chapter_name | string | No |  |
| dry_run | string | No |  |

**Example Request:**
```json
{
  "member_name": "John Doe",
  "chapter_name": "John Doe",
  "dry_run": "example_dry_run"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| membership_type | string | Yes |  |

**Example Request:**
```json
{
  "membership_type": "example_membership_type"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| membership_type | string | Yes |  |

**Example Request:**
```json
{
  "membership_type": "example_membership_type"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| membership_type | string | Yes |  |

**Example Request:**
```json
{
  "membership_type": "example_membership_type"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| membership_type | string | Yes |  |

**Example Request:**
```json
{
  "membership_type": "example_membership_type"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| func | string | Yes |  |

**Example Request:**
```json
{
  "func": "example_func"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| draft_id | integer | Yes |  |

**Example Request:**
```json
{
  "draft_id": 1
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| draft_id | integer | Yes |  |

**Example Request:**
```json
{
  "draft_id": 1
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| member_name | string | Yes |  |
| payment_method | string | Yes |  |
| payment_reference | string | No |  |

**Example Request:**
```json
{
  "member_name": "John Doe",
  "payment_method": "example_payment_method",
  "payment_reference": "example_payment_reference"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| member_name | string | Yes |  |
| reason | string | Yes |  |

**Example Request:**
```json
{
  "member_name": "John Doe",
  "reason": "example_reason"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| data | string | Yes |  |

**Example Request:**
```json
{
  "data": "example_data"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| data | string | Yes |  |

**Example Request:**
```json
{
  "data": "example_data"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| kwargs | string | Yes |  |

**Example Request:**
```json
{
  "kwargs": "example_kwargs"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| kwargs | string | Yes |  |

**Example Request:**
```json
{
  "kwargs": "example_kwargs"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| postal_code | string | Yes |  |

**Example Request:**
```json
{
  "postal_code": "example_postal_code"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| membership_type_name | string | Yes |  |

**Example Request:**
```json
{
  "membership_type_name": "John Doe"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| membership_type_name | string | Yes |  |

**Example Request:**
```json
{
  "membership_type_name": "John Doe"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| data | string | Yes |  |

**Example Request:**
```json
{
  "data": "example_data"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    },
    "data": {
      "type": "object"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| birth_date | string | Yes |  |

**Example Request:**
```json
{
  "birth_date": "example_birth_date"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| birth_date | string | Yes |  |

**Example Request:**
```json
{
  "birth_date": "example_birth_date"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| membership_type | string | Yes |  |
| amount | number | Yes |  |

**Example Request:**
```json
{
  "membership_type": "example_membership_type",
  "amount": 10.0
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| membership_type | string | Yes |  |
| amount | number | Yes |  |

**Example Request:**
```json
{
  "membership_type": "example_membership_type",
  "amount": 10.0
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| email | string | Yes |  |

**Example Request:**
```json
{
  "email": "user@example.com"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| email | string | Yes |  |

**Example Request:**
```json
{
  "email": "user@example.com"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| membership_type | string | Yes |  |
| amount | number | Yes |  |
| uses_custom | string | Yes |  |

**Example Request:**
```json
{
  "membership_type": "example_membership_type",
  "amount": 10.0,
  "uses_custom": "example_uses_custom"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| name | string | Yes |  |
| field_name | string | No |  |

**Example Request:**
```json
{
  "name": "John Doe",
  "field_name": "John Doe"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| name | string | Yes |  |
| field_name | string | No |  |

**Example Request:**
```json
{
  "name": "John Doe",
  "field_name": "John Doe"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| phone | string | Yes |  |
| country | integer | No |  |

**Example Request:**
```json
{
  "phone": "example_phone",
  "country": 1
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| phone | string | Yes |  |
| country | integer | No |  |

**Example Request:**
```json
{
  "phone": "example_phone",
  "country": 1
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| postal_code | string | Yes |  |
| country | integer | No |  |

**Example Request:**
```json
{
  "postal_code": "example_postal_code",
  "country": 1
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| postal_code | string | Yes |  |
| country | integer | No |  |

**Example Request:**
```json
{
  "postal_code": "example_postal_code",
  "country": 1
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| member_name | string | Yes |  |
| membership_type | string | No |  |
| chapter | string | No |  |
| notes | string | No |  |
| create_invoice | string | No |  |

**Example Request:**
```json
{
  "member_name": "John Doe",
  "membership_type": "example_membership_type",
  "chapter": "example_chapter",
  "notes": "example_notes",
  "create_invoice": "example_create_invoice"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| invoice_name | string | Yes |  |

**Example Request:**
```json
{
  "invoice_name": "John Doe"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| member_name | string | Yes |  |

**Example Request:**
```json
{
  "member_name": "John Doe"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    },
    "data": {
      "type": "object"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| member_name | string | Yes |  |

**Example Request:**
```json
{
  "member_name": "John Doe"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| member_name | string | Yes |  |

**Example Request:**
```json
{
  "member_name": "John Doe"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| membership_name | string | Yes |  |

**Example Request:**
```json
{
  "membership_name": "John Doe"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| membership_type_name | string | Yes |  |

**Example Request:**
```json
{
  "membership_type_name": "John Doe"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| chapter | string | No |  |
| days_overdue | string | No |  |

**Example Request:**
```json
{
  "chapter": "example_chapter",
  "days_overdue": "example_days_overdue"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| member_name | string | Yes |  |

**Example Request:**
```json
{
  "member_name": "John Doe"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| kwargs | string | Yes |  |

**Example Request:**
```json
{
  "kwargs": "example_kwargs"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| member_name | string | Yes |  |
| reason | string | Yes |  |
| email_template | string | No |  |
| rejection_category | string | No |  |
| internal_notes | string | No |  |
| process_refund | string | No |  |

**Example Request:**
```json
{
  "member_name": "John Doe",
  "reason": "example_reason",
  "email_template": "user@example.com",
  "rejection_category": "example_rejection_category",
  "internal_notes": "example_internal_notes",
  "process_refund": "example_process_refund"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| kwargs | string | Yes |  |

**Example Request:**
```json
{
  "kwargs": "example_kwargs"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| member_name | string | Yes |  |

**Example Request:**
```json
{
  "member_name": "John Doe"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| period | string | No |  |

**Example Request:**
```json
{
  "period": "example_period"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    },
    "data": {
      "type": "object"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| email_group | string | No |  |

**Example Request:**
```json
{
  "email_group": "user@example.com"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| payment_id | integer | Yes |  |

**Example Request:**
```json
{
  "payment_id": 1
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| year | string | No |  |

**Example Request:**
```json
{
  "year": "example_year"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| member | string | No |  |

**Example Request:**
```json
{
  "member": "example_member"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| member | string | No |  |

**Example Request:**
```json
{
  "member": "example_member"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| member | string | No |  |
| year | string | No |  |
| status | string | No |  |
| kwargs | string | Yes |  |

**Example Request:**
```json
{
  "member": "example_member",
  "year": "example_year",
  "status": "example_status",
  "kwargs": "example_kwargs"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| member | string | No |  |

**Example Request:**
```json
{
  "member": "example_member"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| func | string | Yes |  |

**Example Request:**
```json
{
  "func": "example_func"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| invoice_id | integer | Yes |  |

**Example Request:**
```json
{
  "invoice_id": 1
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| payment_plan_id | integer | Yes |  |
| approval_notes | string | No |  |

**Example Request:**
```json
{
  "payment_plan_id": 1,
  "approval_notes": "example_approval_notes"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| total_amount | number | Yes |  |
| installments | string | Yes |  |
| frequency | string | Yes |  |

**Example Request:**
```json
{
  "total_amount": 10.0,
  "installments": "example_installments",
  "frequency": "example_frequency"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| member | string | No |  |

**Example Request:**
```json
{
  "member": "example_member"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| payment_plan_id | integer | Yes |  |

**Example Request:**
```json
{
  "payment_plan_id": 1
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| payment_plan_id | integer | Yes |  |
| installment_number | integer | Yes |  |
| payment_amount | number | Yes |  |
| payment_reference | string | No |  |

**Example Request:**
```json
{
  "payment_plan_id": 1,
  "installment_number": 1,
  "payment_amount": 10.0,
  "payment_reference": "example_payment_reference"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| payment_plan_id | integer | Yes |  |
| rejection_reason | string | No |  |

**Example Request:**
```json
{
  "payment_plan_id": 1,
  "rejection_reason": "example_rejection_reason"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| member | string | Yes |  |
| total_amount | number | Yes |  |
| preferred_installments | string | No |  |
| preferred_frequency | string | No |  |
| reason | string | No |  |

**Example Request:**
```json
{
  "member": "example_member",
  "total_amount": 10.0,
  "preferred_installments": "example_preferred_installments",
  "preferred_frequency": "example_preferred_frequency",
  "reason": "example_reason"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| action | string | Yes |  |
| apply_to | string | No |  |
| filters | string | No |  |

**Example Request:**
```json
{
  "action": "example_action",
  "apply_to": "example_apply_to",
  "filters": "example_filters"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| filters | string | No |  |
| format | string | No |  |

**Example Request:**
```json
{
  "filters": "example_filters",
  "format": "example_format"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| func | string | Yes |  |

**Example Request:**
```json
{
  "func": "example_func"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| reminder_type | string | No |  |
| include_payment_link | string | No |  |
| custom_message | integer | No |  |
| send_to_chapters | string | No |  |
| filters | string | No |  |

**Example Request:**
```json
{
  "reminder_type": "example_reminder_type",
  "include_payment_link": "example_include_payment_link",
  "custom_message": 1,
  "send_to_chapters": "example_send_to_chapters",
  "filters": "example_filters"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| schema_name | string | Yes |  |
| expose_errors | boolean | No |  |

**Example Request:**
```json
{
  "schema_name": "John Doe",
  "expose_errors": true
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| module_path | string | Yes |  |
| function_name | string | Yes |  |

**Example Request:**
```json
{
  "module_path": "example_module_path",
  "function_name": "John Doe"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| member_names | string | Yes | List of member names or comma-separated string |
| limit | integer | No | Maximum number of members to process (default 10, max 10) |

**Example Request:**
```json
{
  "member_names": "John Doe",
  "limit": 1
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| member_name | string | Yes | Name of member to analyze |

**Example Request:**
```json
{
  "member_name": "John Doe"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| member_name | string | Yes | Name of the member to analyze |

**Example Request:**
```json
{
  "member_name": "John Doe"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| member_name | string | No | Optional member to analyze (None for system-wide only) |

**Example Request:**
```json
{
  "member_name": "John Doe"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| member_count | integer | No | Number of members to test with |

**Example Request:**
```json
{
  "member_count": 1
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| sample_size | integer | No | Number of members to sample for baselines |

**Example Request:**
```json
{
  "sample_size": 1
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| operation_name | string | No | Name for the baseline snapshot |

**Example Request:**
```json
{
  "operation_name": "John Doe"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    },
    "data": {
      "type": "object"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| sample_size | integer | No | Number of members to analyze (default: 10) |

**Example Request:**
```json
{
  "sample_size": 1
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| operation_name | string | No | Filter by specific operation name (optional) |
| limit | integer | No | Maximum number of results to return |

**Example Request:**
```json
{
  "operation_name": "John Doe",
  "limit": 1
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| limit | integer | No | Maximum number of reports to return |

**Example Request:**
```json
{
  "limit": 1
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| member_name | string | Yes | Name of the member to analyze |

**Example Request:**
```json
{
  "member_name": "John Doe"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| member_name | string | Yes | Name of the member |

**Example Request:**
```json
{
  "member_name": "John Doe"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| member_name | string | Yes | Name of the member |

**Example Request:**
```json
{
  "member_name": "John Doe"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| baseline_metrics | object | No | Pre-Phase 5A baseline metrics for comparison |

**Example Request:**
```json
{
  "baseline_metrics": {}
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| days_ahead | string | No | Number of days to look ahead |

**Example Request:**
```json
{
  "days_ahead": "example_days_ahead"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| agreement_name | string | Yes | Periodic Donation Agreement name |

**Example Request:**
```json
{
  "agreement_name": "John Doe"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    },
    "data": {
      "type": "object"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| donor | string | Yes | Donor document name |
| annual_amount | number | Yes | Annual donation amount |
| payment_frequency | string | Yes | Monthly/Quarterly/Annually |
| payment_method | string | Yes | Payment method |
| start_date | string | No | Agreement start date (defaults to today) |
| agreement_type | string | No | Notarial/Private Written |
| sepa_mandate | string | No | SEPA mandate if applicable |

**Example Request:**
```json
{
  "donor": "example_donor",
  "annual_amount": 10.0,
  "payment_frequency": "example_payment_frequency",
  "payment_method": "example_payment_method",
  "start_date": "example_start_date",
  "agreement_type": "example_agreement_type",
  "sepa_mandate": "example_sepa_mandate"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    },
    "data": {
      "type": "object"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| filters | string | Yes | Report filters dict |

**Example Request:**
```json
{
  "filters": "example_filters"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| from_date | string | No | Optional start date filter |
| to_date | string | No | Optional end date filter |

**Example Request:**
```json
{
  "from_date": "example_from_date",
  "to_date": "example_to_date"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| filters | string | Yes | Report filters dict |

**Example Request:**
```json
{
  "filters": "example_filters"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| donor | string | Yes | Donor document name |
| status | string | No | Optional status filter |

**Example Request:**
```json
{
  "donor": "example_donor",
  "status": "example_status"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| donation | string | Yes | Donation document name |
| agreement | string | Yes | Periodic Donation Agreement name |

**Example Request:**
```json
{
  "donation": "example_donation",
  "agreement": "example_agreement"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| days_before_expiry | string | No | Days before expiry to send reminders |

**Example Request:**
```json
{
  "days_before_expiry": "example_days_before_expiry"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| test_user_email | string | Yes |  |

**Example Request:**
```json
{
  "test_user_email": "user@example.com"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| force_enable | boolean | No |  |

**Example Request:**
```json
{
  "force_enable": true
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    },
    "data": {
      "type": "object"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| force_enable | boolean | No |  |

**Example Request:**
```json
{
  "force_enable": true
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| operation_type | string | Yes | Operation type to configure |
| custom_ttl | integer | No | Custom TTL in seconds |

**Example Request:**
```json
{
  "operation_type": "example_operation_type",
  "custom_ttl": 1
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| doctype | string | Yes | DocType that changed |
| doc_name | string | No | Specific document name (optional) |

**Example Request:**
```json
{
  "doctype": "example_doctype",
  "doc_name": "John Doe"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| user | string | No | User to invalidate cache for |
| operation_types | array | No | List of operation type names to invalidate |

**Example Request:**
```json
{
  "user": "example_user",
  "operation_types": []
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| hours_back | integer | No |  |

**Example Request:**
```json
{
  "hours_back": 1
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| date_range | string | No | "Last Month", "Last 3 Months", "Last Year" |

**Example Request:**
```json
{
  "date_range": "example_date_range"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| member_list | string | Yes |  |
| suspension_reason | string | Yes |  |
| suspend_user | string | No |  |
| suspend_teams | string | No |  |

**Example Request:**
```json
{
  "member_list": "example_member_list",
  "suspension_reason": "example_suspension_reason",
  "suspend_user": "example_suspend_user",
  "suspend_teams": "example_suspend_teams"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| member_name | string | No |  |

**Example Request:**
```json
{
  "member_name": "John Doe"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| limit | string | No |  |
| offset | string | No |  |
| status | string | No |  |
| chapter | string | No |  |

**Example Request:**
```json
{
  "limit": "example_limit",
  "offset": "example_offset",
  "status": "example_status",
  "chapter": "example_chapter"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| member_name | string | Yes |  |

**Example Request:**
```json
{
  "member_name": "John Doe"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| member_name | string | Yes |  |

**Example Request:**
```json
{
  "member_name": "John Doe"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| member_name | string | No |  |

**Example Request:**
```json
{
  "member_name": "John Doe"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| func | string | Yes |  |

**Example Request:**
```json
{
  "func": "example_func"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| member_name | string | Yes |  |
| suspension_reason | string | Yes |  |
| suspend_user | string | No |  |
| suspend_teams | string | No |  |

**Example Request:**
```json
{
  "member_name": "John Doe",
  "suspension_reason": "example_suspension_reason",
  "suspend_user": "example_suspend_user",
  "suspend_teams": "example_suspend_teams"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| member_name | string | Yes |  |
| unsuspension_reason | string | Yes |  |

**Example Request:**
```json
{
  "member_name": "John Doe",
  "unsuspension_reason": "example_unsuspension_reason"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| member_name | string | Yes |  |
| termination_type | string | Yes |  |
| termination_date | string | No |  |
| request_name | string | No |  |

**Example Request:**
```json
{
  "member_name": "John Doe",
  "termination_type": "example_termination_type",
  "termination_date": "example_termination_date",
  "request_name": "John Doe"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| member_name | string | Yes |  |

**Example Request:**
```json
{
  "member_name": "John Doe"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| member_name | string | Yes |  |

**Example Request:**
```json
{
  "member_name": "John Doe"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| member_name | string | No |  |

**Example Request:**
```json
{
  "member_name": "John Doe"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| member_name | string | No |  |

**Example Request:**
```json
{
  "member_name": "John Doe"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| member_name | string | No |  |

**Example Request:**
```json
{
  "member_name": "John Doe"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| volunteer_name | string | Yes | Name of the volunteer |
| limit | string | No | Maximum number of recommendations |

**Example Request:**
```json
{
  "volunteer_name": "John Doe",
  "limit": "example_limit"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object"
      }
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| workspace_name | string | No |  |

**Example Request:**
```json
{
  "workspace_name": "John Doe"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    },
    "data": {
      "type": "object"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    },
    "data": {
      "type": "object"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| operation | string | Yes |  |

**Example Request:**
```json
{
  "operation": "example_operation"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| workspace_name | string | Yes |  |

**Example Request:**
```json
{
  "workspace_name": "John Doe"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| workspace_name | string | No |  |

**Example Request:**
```json
{
  "workspace_name": "John Doe"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

## {file_name}

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Parameters:**

| Name | Type | Required | Description |
|------|------|----------|-------------|
| workspace_name | string | No |  |

**Example Request:**
```json
{
  "workspace_name": "John Doe"
}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---

### {endpoint['function_name']}

**Description:** {endpoint['description']}

**Endpoint:** `POST {endpoint['path']}`

**Authentication:** Required


**Example Request:**
```json
{}
```

**Example Response:**
```json
{
  "type": "object",
  "properties": {
    "valid": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    }
  }
}
```

---
