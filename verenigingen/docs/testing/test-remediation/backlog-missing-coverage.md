# Backlog — Missing coverage / features from deleted aspirational tests

Intended behaviors from tests deleted because they referenced nonexistent endpoints/features.
Format: `- <intended behavior> — from <deleted test> — build feature OR write real test.`

- PaymentContext (in `payment_context_resolver.py`) validation of empty/invalid `payment_type` / `target_doctype` / `target_name` — from deleted `test_payment_context_resolver.py::TestPaymentContextResolver.test_context_validation`. The deleted test built `PaymentContext("", "Donation", "test")` etc. and left the assertions as a TODO comment ("Note: Actual validation behavior depends on implementation... ready for when validation is added"). `PaymentContext.__init__` is a bare data container with zero validation logic today. Build the feature (reject/flag empty fields) OR write a real test once validation exists.

## Module 4 — Member-financial & history

- Simple percentage-based membership fee discount (e.g. "Student = 50% off base fee") — from deleted/replaced `test_membership_fee_calculation_edge_cases` (`test_member_lifecycle_unit.py`). Confirmed via `verenigingen/services/member/approval/application_payments.py:calculate_membership_amount_with_discounts` (line 329) that the legacy `student_discount_percentage`/`early_bird_discount` fields **do not exist** on the current Membership Type DocType — its own docstring says discounting moved entirely to the Dues Schedule Template / contribution system. No equivalent simple-percentage test exists for the current mechanism. Replaced with a real bounded-unhappy test of `MemberFeeValidationService.validate_fee_override_amount()` instead (a genuine, currently-tested gate). If discount-by-membership-type coverage is wanted, write a real test against `calculate_membership_amount_with_discounts()` or the Dues Schedule Template system directly.
- Postal-code → region/chapter assignment reimplemented as a toy 4-bucket dict (`test_chapter_assignment_validation`, `test_real_chapter_assignment_from_postal_code`) — from deleted `test_member_lifecycle_unit.py` method and deleted `test_member_lifecycle_mock_elimination.py` (whole file). Real assignment logic lives in `verenigingen/services/chapter/chapter_matching_service.py` (`ChapterMatchingService.get_chapters_by_postal_code`/`suggest_chapters_for_member`), which requires real Chapter/postal-code-range fixtures and is already covered under `verenigingen/tests/chapter/test_chapter_service_coverage.py` (Module 7's scope, out of bounds here). No action needed unless that module's coverage is found lacking.
- "Same person, different email" / "same email, moved address" duplicate-member detection — from deleted `test_real_duplicate_detection_business_logic` (`test_member_lifecycle_mock_elimination.py`). No production duplicate-detection service was found beyond the plain email-uniqueness check (now covered for real by `test_email_validation_and_uniqueness` via `application_validators.validate_email()`). If cross-field duplicate detection (same birth_date + postal_code, different email) is an intended feature, it needs to be built and then tested for real — nothing to call today.
- **Billing-date rejection path** (named OFFSET CANDIDATE, not filled): searched `verenigingen/services/billing/billing_date_service.py` (`set_billing_day`, `advance_schedule_dates`, `initialize_next_invoice_date`) for a genuine raise/reject on an invalid billing day/date; found only graceful defaulting (missing `member_since` → default billing_day=1) and error-recovery date-advancing, no rejection gate. Did not find a bounded, pure-unit-testable genuine-raise target within this module's scope in the time available — flagging for a follow-up look at `membership_dues_schedule.py`'s save-time validation (co-located doctype, Module 6/Wave 4 territory) or the invoice-generation orchestrator.
- **Payment-history real-DB rejection path** (named OFFSET CANDIDATE, not filled): searched `services/member/payment/payment_history_service.py`, `utils/payment_history_validator.py`, and the `Member Payment History` doctype controller (`member_payment_history.py` — an empty `pass` controller); found only repair/report/graceful-fallback logic (`refresh_financial_history` returns an `OperationResult`, never raises on bad data). No genuine raise/throw gate found within the time available — flagging for follow-up if a real rejection path exists elsewhere (e.g. invoice/payment reconciliation validators not yet grepped).

## Module 6 — Membership/termination

- **Exclusive "default membership type" is enforced only client-side, not server-side** — from deleted `test_default_membership_type` (`membership_type/test_membership_type.py`). The deleted test explicitly documented (in a comment) that "the business logic for ensuring only one default doesn't appear to be implemented" and asserted two simultaneously-`default_for_new_members=1` Membership Types as the current (tautological — it asserts the flags it just set) behavior. Confirmed: `membership_type.py`'s `validate()` never references `default_for_new_members` at all — the ONLY enforcement is in `membership_type.js`'s `default_for_new_members(frm)` client-script handler, which prompts the user via `frappe.confirm()` and unsets the other type's flag via `frappe.client.set_value`. This is a **CLAUDE.md "business logic is server-side" violation**: any doc created via the REST API, Data Import, `bench console`/scripts, or the `members_without_dues_schedule` report's `{"default_for_new_members": 1}` filter can end up with multiple simultaneous defaults, since nothing server-side prevents it. If exclusivity is actually required, add a `validate()` guard in `membership_type.py` (unset any other type's flag, or throw) and replace the deleted test with a real assertion against that guard; otherwise leave as documented multi-default-allowed behavior.

## Module 1 — SEPA

- Automatic SEPA Audit Log entry on SEPA Mandate creation/status-change — from deleted `test_mandate_audit_logging` (`test_sepa_mandate_comprehensive.py`). `SEPAAuditLog.log_mandate_creation()`/`log_sepa_event()` are real, callable, and unit-tested directly (see rewritten `test_sepa_audit_log.py`), but `verenigingen/hooks/doc_events.py`'s `"SEPA Mandate"` entry only wires cache-invalidation hooks (`after_save`/`on_submit`/`on_cancel`/`on_trash` → `cache_invalidation.*`) — nothing calls `SEPAAuditLog.log_*` automatically on mandate save. If an automatic compliance audit trail is intended, wire it into `doc_events.py`; otherwise this is dead intent.
- SEPA Mandate data-protection/access-audit assertions — from deleted `test_mandate_data_protection_compliance`. No field-level masking or perm_level restriction exists on `iban`/`account_holder_name`/`bic` in `sepa_mandate.json`; ownership access control is already covered for real by `test_mandate_permission_member_ownership`. No distinct feature gap beyond the audit-trail item above.
- Mollie-specific SEPA Mandate creation coupling — from deleted `test_mollie_integration_mandate_creation`. Grepped `verenigingen/verenigingen_payments/mollie/` for any `frappe.new_doc("SEPA Mandate")`/`frappe.get_doc({"doctype": "SEPA Mandate"...})` call: none exists. There is no real integration point where a Mollie flow creates or touches a SEPA Mandate document; if one is intended (e.g. a Mollie-initiated mandate), build it and write a real test against that path.
- Mandate non-use expiry (e.g. auto-expire a mandate unused for 36 months) — from deleted `test_mandate_expiry_validation` (`test_sepa_business_logic_unit.py`). `SEPAMandate.set_status_based_on_dates()` only expires a mandate from its explicit `expiry_date` field; there is no usage-history-derived "expired due to non-use" rule anywhere in `sepa_mandate.py` or `sepa_mandate_lifecycle_service.py`.
- SEPA Mandate is not covered by `DataRetentionPolicy._category_to_doctype()` (`verenigingen_payments/core/compliance/data_retention_policy.py`) — from deleted `test_mandate_data_retention_compliance`. The mapping only covers `Payment Entry` / `Member` / `Sales Invoice` / `Mollie Audit Log` / `Journal Entry`; SEPA Mandate (which holds IBAN PII) has no retention/anonymization policy. Extend the mapping if SEPA Mandate PII needs retention compliance.
- Generic SEPA payment-amount ceiling (positive, ≤2 decimals, ≤ EUR 1,000,000) — from deleted `test_payment_amount_validation`. No such universal validator exists; the only real amount check is `SEPAMandateUsage.validate_amount()`, which only compares against that specific mandate's own `maximum_amount` field.
- SEPA payment-batch execution-date validation (reject a batch dated in the past) — from deleted `test_payment_batch_validation`. `DirectDebitBatch.validate()`/`validate_invoices()`/`validate_sequence_types()` never check `batch_date` against today.
- Dutch SEPA creditor-identifier (Incassant-ID) format validation — from deleted `test_creditor_identifier_validation`. No validator for the `NL##ZZZ#########` format exists anywhere in the SEPA codebase.
- SEPA payment purpose-code (ISO 20022, e.g. `CBFF`/`GOVI`) validation — from deleted `test_payment_purpose_code_validation`. No such validator exists.
- SEPA batch-booking-indicator (`BtchBookg`) decision logic — from deleted `test_batch_booking_validation`. Confirmed in `sepa_xml_enhanced_generator.py`/`sepa_xml_adapter.py`: `batch_booking` is hardcoded `True` on every `PaymentInformation`, never derived from payment count as the deleted test assumed.
- SEPA XML file-reference/message-ID generation format — from deleted `test_file_reference_generation`. Real message-ID generation lives in `sepa_xml_adapter.py`/`sepa_xml_enhanced_generator.py` with its own scheme; the deleted test's `PREFIX-YYYYMMDD-HHMMSS-NNN` format was purely local invention with no production counterpart.
- Member deletion cascade does not handle linked SEPA Mandates — NOT a deleted test, but a real gap the rewritten `test_mandate_cleanup_on_member_deletion` (`test_sepa_mandate_comprehensive.py`) now documents and pins: `MemberCleanupService.handle_member_deletion()` (`services/member/lifecycle/member_cleanup_service.py`) clears the Member's "Member SEPA Mandate Link" child table but never deletes/cancels/unlinks the `SEPA Mandate` document itself (whose `member` field still points at the Member). Deleting a Member with an active mandate therefore raises `frappe.LinkExistsError` (confirmed empirically via `bench execute`). If cascading cleanup should also handle mandates (e.g. cancel + unlink, mirroring the Membership/Chapter Member handling above it), extend `handle_member_deletion()` and update the pinned test to assert success instead.
