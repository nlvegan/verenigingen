# Backlog — Dead production code found while remediating tests

Prod symbols with no callers, surfaced when a test's target turned out unreachable.
Format: `- <file>:<symbol> — from <deleted test> — re-grep callers before deleting prod code.`

- `verenigingen/verenigingen_payments/mollie/services/payment_context_resolver.py:PaymentContextResolver._resolve_from_document_lookup` (line ~167) queries `frappe.db.get_value("Member", {"payment_id": payment_id}, "name")`, but the **Member doctype has no `payment_id` field** (only Donation does — confirmed via `member.json` vs `donation.json`). This raises `MySQLdb.OperationalError: Unknown column 'payment_id' in 'WHERE'` on every call where the preceding Donation lookup also misses. The exception is masked by `resolve_context()`'s outer `except Exception: return None`, so callers never see a crash — but it means **Strategy 4 (`_resolve_from_customer_fallback`, the customer-ID + timestamp window fallback) is unreachable in practice** whenever a payment isn't resolved by metadata/subscription/donation-payment_id lookup. Surfaced while rewriting `test_payment_context_resolver.py`'s assertion-free tests (all 5 rewritten tests return None via this masked path, not via a clean "no match" fallthrough — see their docstrings). This is a live bug, not literally dead code, but the practical effect (strategy 4 never runs) is the same — **needs a real prod fix** (drop the bogus Member.payment_id branch, or add the field) before Strategy 4 becomes reachable. NOT fixed here (test-files-only mandate); flagging for controller/dedicated-pass review.

## Module 4 — Member-financial & history

- `verenigingen/services/member/core/member_status_service.py:validate_status_transition` (line 193) — a real, correctly-implemented status-transition validator (`allowed_transitions` dict + lookup) with **no production caller found**. Grepped every import of `member_status_service` across the app: only `get_member_status_color`, `set_member_application_status_defaults`, `update_member_membership_status`, and `sync_member_status_fields` are actually wired into the Member save lifecycle (`member_before_save_service.py`, `member_validation_service.py`). `validate_status_transition` is imported only by two test files (`tests/unit/test_member_lifecycle_unit.py` — rewritten here to call it for real instead of reimplementing its table inline; and `tests/services/test_member_status_service.py`, out of this module's scope). Re-grep callers before any deletion pass — it may be intended for a future status-change guard that was never actually hooked up (`before_save`/`validate` never calls it), which would make this a **missing-wiring** issue rather than pure dead code.

## Module 1 — SEPA

- `verenigingen/verenigingen_payments/services/sepa_batch_processor.py:add_invoices_to_batch_optimized` (line ~230) — the optimizer-failure handler calls `frappe.log_error(message, title)` positionally, but this Frappe version's signature is `log_error(title=None, message=None, ...)` (`frappe/utils/error.py:37`). **The arguments are swapped**: the long "Performance optimizer failed, falling back..." string lands in the `method` (title) column and the intended title "SEPA Performance Optimizer Error" lands in the `error` (message) column. Not a crash (log_error never raises), but the Error Log's `method` field — normally used to group/search by error type — is polluted with a unique per-exception message instead of a stable title, breaking searchability. Surfaced while rewriting `test_sepa_integration_performance.py::test_performance_degradation_handling` (the rewritten test matches on content in either field to stay correct regardless of the swap). NOT fixed here (test-files-only mandate); a one-line argument-order fix (`frappe.log_error("SEPA Performance Optimizer Error", f"Performance optimizer failed...: {e}")`) would restore correct grouping.

## Module 6 — Membership/termination

- `verenigingen/verenigingen/doctype/membership_termination_request/membership_termination_request.py:on_submit` / `on_update_after_submit` (→ `handle_status_change`, wired via `hooks/doc_events.py`) — **unreachable via the real production lifecycle**. `on_update_after_submit` only fires when Frappe's `_action` resolves to `"update_after_submit"`, which requires the document to already be `docstatus == 1` (submitted) — confirmed by tracing `Document.save()`/`set_docstatus()` in `frappe/model/document.py`. But the only two places that ever drive this doctype's status forward — `TerminationApprovalService.submit_for_approval()` and `.approve_request()` (`verenigingen/services/approval/termination_approval_service.py`) — call `self.request.save()`, **never `.submit()`**; grepped the whole app for `.submit()` on a `Membership Termination Request` doc and found none in production code. So in real usage `docstatus` never leaves 0, `on_update_after_submit`/`on_submit` never run, and status → "Executed" via the approval workflow's `save()` never triggers `execute_termination_internal()` through the doc lifecycle — only the separate whitelisted `execute_termination()` doc method / `execute_termination_request()` module function (which call `TerminationExecutionService` directly) actually execute a termination. Confirmed empirically via `bench console` (`.submit()` *can* be called manually and does flip docstatus — `is_submittable: 0` in the JSON is form-layer only — but nothing in prod code does so). Surfaced while rewriting `test_membership_termination_request.py`'s status-transition tests (previously false-confidence db round-trips): the replacement test now targets the one REAL per-save side effect (`before_save`'s `TerminationAuditService.log_document_update`, which fires on every save regardless of docstatus) instead. If `on_submit`/`handle_status_change` are meant to be reachable, either wire the approval service to call `.submit()`, or remove the dead hook wiring; not fixed here (test-files-only mandate).

## Module 8 — Report/api/portal

- `verenigingen/verenigingen/report/overdue_member_payments/overdue_member_payments.py:is_membership_related` (line 313) and its **duplicate** `verenigingen/verenigingen_payments/report/overdue_member_payments/overdue_member_payments.py:is_membership_related` (line 242) — both are hardcoded `return True` stubs ("Updated to use dues schedules instead of subscriptions. Always returns True for backward compatibility"). Grepped every call site of `is_membership_related` across `verenigingen/` (prod + tests): **zero callers**, not even from within their own module's `execute()`/`get_data()` — the function is defined but never invoked anywhere. Deleted `test_is_membership_related_always_true` (`tests/report/test_overdue_member_payments.py`), which only asserted the hardcoded return value of a function nothing calls. If this was meant to gate invoice inclusion by membership-relatedness, that wiring was apparently dropped when the dues-schedule migration landed — worth checking whether `get_data()` should be filtering by it. Not fixed here (test-files-only mandate); also note the **duplicate function in two files** (`verenigingen/verenigingen/report/...` vs `verenigingen/verenigingen_payments/report/...`) is itself a DRY smell worth a follow-up look.

## Module 7 — Chapter/volunteer/donation/donor

- `Volunteer Expense` DocType (archived in commit `1a8e5fa2`; table dropped by `patches/v2_2/drop_volunteer_expense_archived_doctype.py`) and its now-nonexistent permission helpers `verenigingen.permissions.has_volunteer_expense_permission`, `get_volunteer_expense_permission_query`, `can_approve_volunteer_expense`, and `verenigingen.utils.chapter_role_events.before_volunteer_expense_submit` — confirmed via `grep -rn` that none of these four symbols exist anywhere in production code any more (only referenced inside `try/except ImportError` fallbacks in the deleted tests, which is itself a tell). Deleted 8 `@unittest.skip(VOLUNTEER_EXPENSE_ARCHIVED)` tests from `tests/chapter/test_chapter_board_permissions_comprehensive.py` (`test_scenario_a_happy_path_treasurer_workflow`, `test_scenario_b_cross_chapter_security`, `test_scenario_c_role_lifecycle`, `test_scenario_d_non_treasurer_board_member`, `test_expense_workflow_edge_cases`, `test_performance_permission_queries`, `test_security_privilege_escalation_prevention`, `test_complete_workflow_integration`) and 3 from `tests/chapter/test_chapter_board_permissions.py` (`test_volunteer_expense_chapter_filtering`, `test_treasurer_expense_approval`, `test_expense_approval_workflow_validation`). Kept the 4 non-skipped tests in the same classes that do not touch Volunteer Expense (`test_membership_termination_workflow`, `test_orphaned_board_member_handling`, `test_chapter_role_validation`, `test_board_member_field_validation`). Removed the now-unused `VOLUNTEER_EXPENSE_ARCHIVED` import from both files.
