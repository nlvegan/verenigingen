# Known (pre-existing) Server Tests failures — the "no-new-failures" gate baseline.
# Consumed by scripts/testing/check_new_test_failures.py: a PR fails the gate
# only if it produces a failing test NOT listed here. Lines starting with # are
# ignored. Includes test methods AND fixture-hook failures (setUpClass/setUpModule).
#
# ***** THIS BASELINE IS INTENTIONALLY EMPTY (2026-07-26). *****
# Every entry was either confirmed stale or fixed. The gate is therefore fully
# armed: ANY failing test now fails the gate. Do not add an entry here to make a
# red test go away without recording why below.
#
# How it got to zero, from 32:
#
# 1. 32 -> 13 by regenerating against two green develop CI runs, all 12 shards
#    each: run 30211441005 (f28741ae, after PR #186) and run 30076384474
#    (8cc0a969). Every one of the 32 was confirmed to have actually RUN in both
#    runs, so nothing was pruned on absence-of-evidence. Of the 19 dropped, 7
#    passed in BOTH runs (simply stale), and 12 passed only in the newer run
#    because PR #186 fixed them: the 9 test_volunteer_api entries via
#    grant_matching_role_profiles() in setUp (the @high_security_api endpoints
#    need a role PROFILE, not a bare role — issue #162), and the 3
#    test_mollie_subscription_consolidation entries via the frappe.db.begin()
#    removal in complete_payment_service.py.
#
# 2. 13 -> 0 by fixing the rest rather than baselining them. Two were product
#    bugs, not test bugs:
#      * _process_subscription_payment stamped the Payment Entry with the company
#        from frappe.defaults.get_user_default("Company") instead of the invoice's
#        own company, so ERPNext looked for the invoice among the WRONG company's
#        outstanding documents and threw "has already been fully paid" for a
#        fully-outstanding invoice. Broke test_race_condition_protection.
#      * The applicant-facing validate_birth_date pre-check accepted any age >= 0
#        while the save enforced the configured minimum (16), so the application
#        form green-lit an under-age applicant the submission would then reject.
#        Broke test_age_validation.
#
# The other 11 were test-side: a wrong mock target (get_password vs
# get_webhook_secret); an assertion about an application_status the factory never
# sets; two preconditions that assumed ambient site state (Global Defaults
# .default_company empty; Mollie Settings.mollie_clearing_account populated); an
# outgrown query budget (441 queries / 10 saves, identical locally and in both CI
# runs — NOT an N+1); three concurrency tests whose worker threads could not see
# the parent's uncommitted fixtures; and three CI-only environment differences
# (developer_mode off; a module-level singleton's memoised settings; a Redis-cached
# is_dutch_installation flag).
#
# LESSONS worth keeping, all of which cost real time here:
#   * Match on the FULLY-QUALIFIED dotted path, never the bare method name.
#     test_dutch_name_handling_with_tussenvoegsel exists on BOTH
#     TestMemberLifecycleWorkflows and TestSEPAXMLCompliance, in different shards,
#     one failing and one passing.
#   * Per-test result lines carry a "(2.49s)" suffix for slow tests only; a parser
#     that ignores it silently drops those results and reports them as "not run".
#   * A test absent from a shard's failure list has NOT necessarily passed —
#     confirm it actually ran before pruning it.
#   * A green LOCAL module run is not evidence a CI failure is fixed. Five of these
#     passed locally and failed in every shard. Reproduce the CI condition first
#     (developer_mode=0, cleared setting, poisoned cache), watch it fail, then fix.
#
# If a test starts failing and you believe it is a pre-existing/accepted failure
# rather than your regression, re-run the job first. To re-baseline legitimately:
#   for each shard log: check_new_test_failures.py --results <log> --emit-baseline >> b.txt
#   then `sort -u`, re-add this header, and record WHY each entry is here.
